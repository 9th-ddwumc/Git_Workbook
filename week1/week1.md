# week1 정리

## Git 기본 명령어
- `git clone`: 원격 저장소를 복제한다.
- `git branch`: 현재 브랜치를 확인한다.
- `git checkout -b`: 새로운 브랜치를 생성하고 이동한다.

## 배운 점
- `git clone` : 원격 저장소를 복제
- `git branch` : 브랜치 확인
- `git checkout -b` : 새 브랜치 생성 후 이동

## 배운 점
- 깃 브랜치를 생성해서 안전하게 과제를 관리할 수 있다.
- PR을 통해 협업 방식에 대해 이해했다.
